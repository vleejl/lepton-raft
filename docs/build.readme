# 1. 带线程检测的debug构建
./build.sh --clean -m debug -s asan
./build.sh -m debug -s tsan

# 2. 清理后执行release构建
./build.sh --clean --mode release

# 3. 显示帮助信息
./build.sh -h

# 4. 格式化所有代码，排除build和third_party目录
./format_all.sh -e build,third_party

# 5. clang 编译
./build.sh -m debug -t clang -s tsan
./build.sh -m debug -t clang -s asan
./build.sh --clean -m debug -t clang -s asan -j 32
